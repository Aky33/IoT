# Public API base URL used by frontend runtime requests.
# Leave empty to use same-origin requests.
VITE_API_BASE_URL=

# Backend target used by Vite dev proxy.
# If not set, falls back to VITE_API_BASE_URL and then http://localhost:3000.
VITE_DEV_PROXY_TARGET=http://localhost:3000